<!-- THIS IS NOT USED DIRECTLY - JUST A TEMPLATE FOR STYLES -->

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:ComponentModel="clr-namespace:System.ComponentModel;assembly=PresentationFramework"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mwt1="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">

    <SolidColorBrush x:Key="MainBackgroundBrush" Color="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"/>
    <SolidColorBrush x:Key="MainTextBrush" Color="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"/>

    <Style x:Key="TextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource MainTextBrush}" />
        <Style.Triggers>
            <!-- Below makes text white in Dark Mode VS Design Time -->
            <Trigger Property="ComponentModel:DesignerProperties.IsInDesignMode" Value="true">
                <Setter Property="Foreground" Value="#e0e0e0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>